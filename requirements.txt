# Dependencies for this repo's own scripts.
#
# Most scripts (prepare_dataset.py, verify_dataset.py, maqam_prompt_generator.py,
# audition_planner.py, backup_to_gcp.py) are stdlib-only. render_tokens_nar.py
# additionally imports torch and safetensors, which are installed by ComfyUI and
# ComfyUI-YuE2-Trainer (both pinned by commit in bootstrap/setup.sh). The
# versions below are the ones this project was verified against -- pinning them
# makes a drifting environment visible instead of silent.
numpy==2.1.3
safetensors==0.8.0
